defaults:
  - agent_rollout_rock_swe_traced
  - _self_

exp_name: "agentic_rollout_swe_native_roll"

actor_infer:
  strategy_args:
    strategy_config:
      # Native baseline: no LMCache-backed reload path.
      # Keep vLLM native prefix caching on for a fairer baseline.
      enable_prefix_caching: true
      lmcache_config: null

agentic_kv:
  enable: false
